# 好事发生 · 实施指南（v1.0）

> **定位**：技术决策 + 改造范围 + 验收标准 + 分步执行清单。无代码路径依赖。
> **前置文档**：`产品需求规格说明书-SPEC-v4.md`
> **决策日期**：2026-06-19
>
> **给 TRAE AI 编程 agent 的说明：**
> 本指南告诉你「怎么决策、改哪些模块、做到什么标准算完成、按什么顺序做」。
> 实施前请先阅读 SPEC v4 了解全量需求，再回到本指南进行任务拆解和代码实现。
> 所有技术选型（React版本、数据库类型、API端点命名、文件组织）由 TRAE 自行确定。

---

## 第一章：技术决策与改动全貌

### 1.1 六大设计决策

| # | 决策 | 核心原因 | 优先级 |
|:---:|------|------|:---:|
| 1 | **排行榜 → 温暖聚合** | 过度理由效应：排名把"我是善良的人"变成"我要冲前十" | P0 |
| 2 | **即时正反馈 <3秒** | 游戏正反馈科学：帮人→立刻被温暖→想再做一件 | P0 |
| 3 | **排名表彰 → 温暖故事发布** | 道德活动不应有输家；聚光灯不比较，排行榜会比较 | P0 |
| 4 | **福气计算五因子公式** | Base×Type×Trust×Streak+Bonus，精细化奖励 | P1 |
| 5 | **三端差异处理** | Web先行全量 → 小程序同步核心 → App后补 | P1 |
| 6 | **旧数据软处理** | 字段重命名保留数据、旧API标记废弃不删除 | P0 |

### 1.2 改动范围总览

| 层 | 新增 | 修改 | 废弃处理 |
|------|:---:|:---:|------|
| 数据库 | 字段 ×3、索引 ×1 | 字段重命名 ×1、配置项变更 | 回滚脚本备份 |
| 后端 | 服务 ×5（聚合/统计/福气/连续日/里程碑） | 善行发布接口增强、AI共鸣改为异步、排行榜标记废弃 | 旧接口保留兼容 |
| Web前端 | 温暖聚合页、温暖故事卡片、福气动画、AI共鸣卡片、里程碑弹出、温暖地图 | 导航栏、首页、发布页、设置页、统计页 | 排行榜页重定向 |
| 小程序 | 同Web新增 | 同Web修改 + 订阅消息 | 排行榜页重定向 |
| App | 同Web新增 | 同Web修改 + App推送 | 排行榜页重定向 |
| 管理后台 | 温暖故事标记、温暖故事管理 | 配置中心（移除排行榜参数） | - |

### 1.3 技术风险与缓解

| 风险 | 缓解 |
|------|------|
| AI共鸣 > 3秒 | 异步模式 + 占位UI + fallback文案 |
| 聚合查询慢 | 定时缓存刷新（15分钟）+ 索引优化 |
| 福气计算复杂 | 互动加成异步计算，主流程只算基础分 |
| AI真实性评判不稳定 | 默认1.0系数，保守下发 |
| 旧客户端报错 | 标记废弃不删除 + 重定向 |

---

## 第二章：各模块验收标准

### 2.1 温暖聚合（P0）

| 验收项 | 标准 |
|------|------|
| 页面无排名 | 全页不出现「排名」「第X名」「TopN」等字样 |
| 聚合数据正确 | 与SQL直接查询结果一致，只统计公开+审核通过+参与聚合的用户 |
| 隐私保护 | 人数<10时 `participant_count = null`，前端显示"10人以内" |
| 仅自己可见 | 个人统计面板退出登录后不可见，面板顶部有🔒标识 |
| 区域切换 | 切换地区后数据正确更新 |
| 温暖故事 | 被标记的善行出现在故事列表，发布者匿名展示 |
| 响应速度 | 聚合查询 < 200ms（缓存命中） |
| 温暖地图 | 中国地图色温图可用，不显示排名 |

### 2.2 即时正反馈（P0）

| 验收项 | 标准 |
|------|------|
| 反馈总时长 | < 3秒完成全部反馈序列（含AI响应） |
| 发布不阻塞 | 提交善行到反馈卡片出现 < 1秒 |
| AI异步 | AI超时不阻塞操作，5秒后显示fallback文案 |
| 降级覆盖 | AI超时/报错 → graceful降级，不报错不卡死 |
| 动效完整 | 福气飘字→周计数→AI卡片→流式输出序列完整 |
| 里程碑触发 | 跨称号阈值时弹出庆祝，不过度打扰 |
| 跨端一致 | Web/小程序/App体验一致 |

### 2.3 福气计算（P1）

| 验收项 | 标准 |
|------|------|
| 公式正确 | `min(30, max(0, round(Base×Type×Trust×Streak) + Bonus))` |
| 每日上限 | 第6条起福气=0；日总和 ≤ 60 |
| 连续日 | 中断一天归零；7天Streak=1.05/30天=1.15 |
| AI真实性 | 高1.2/中1.0/低0.5 三档正确映射 |
| 称号不降级 | 扣减后保留曾达到的最高称号 |

---

## 第三章：分步执行清单

> 按 Phase 顺序执行。每一步描述"做什么 + 验收是什么"，具体文件定位和代码实现由 TRAE 自行完成。

### Phase 0：数据库迁移（0.5天 | 必须先做）

**Step 0.1**：字段重命名
- `show_in_leaderboard` → `show_in_aggregation`（用户表RENAME COLUMN）
- 验收：旧字段不存在 / 新字段默认值=1

**Step 0.2**：新增温暖故事标记字段
- `deeds` 表新增 `is_warmth_featured`（BOOL, 默认0）+ 索引
- 验收：字段+索引存在

**Step 0.3**：新增连续天数统计字段
- `users` 表新增 `streak_days`（INT, 默认0）+ `last_deed_date`（DATE）
- 验收：字段存在

**Step 0.4**：全局搜索替换 `show_in_leaderboard` → `show_in_aggregation`
- 搜索范围：后端/前端/小程序/App/管理后台所有代码
- 验收：全局搜索无残留（文档和迁移脚本除外）

**Step 0.5**：创建回滚SQL脚本
- 验收：回滚脚本可执行反向操作

---

### Phase 1：后端 Warmth 模块（1天 | 依赖 Phase 0）

**Step 1.1**：温暖聚合查询服务

功能列表：
- 全国聚合统计（按时间段）→ 返回参与人数/善行量/温暖值
- 区域分组聚合（按时间段）→ 按区域分组，每组人数<10返回null
- 话题聚合 → 指定话题的统计数据
- 个人温暖统计（鉴权）→ 返回个人周/月/累计数据、称号、类型分布

核心约束：
- 缓存15分钟
- `participant_count < 10 → null + note`
- 只统计 `status=approved AND is_public=1 AND show_in_aggregation=1`

验收：各聚合接口返回正确 / 人数<10隐私保护 / 缓存机制生效

**Step 1.2**：温暖聚合API路由

三个端点：
- `GET /warmth/stats` — 聚合统计（公开）
- `GET /warmth/personal` — 个人统计（需鉴权，仅返回自己）
- `GET /warmth/map` — 全国温暖地图数据（公开）

验收：接口可用 / 鉴权正确 / 响应 < 200ms

**Step 1.3**：标记旧排行榜路由废弃
- 不删除代码，添加 `@deprecated` 注释
- 保留向后兼容

验收：旧接口仍可访问 / 新功能通过新接口

**Step 1.4**：福气计算引擎

实现五因子公式：
```
fortune = min(30, max(0, round(Base × Type × Trust × Streak) + Bonus))
约束：日条数≤5 / 日总和≤60 / 单条≤30
```

系数表（可配置）见 SPEC §C1。

验收：各系数组合正确 / 每日上限生效 / 单条上限生效

**Step 1.5**：连续日统计

逻辑：
- 今天有记录 → last_deed_date=昨天 → streak_days+1
- last_deed_date=更早 → streak_days=1
- 同一天多次 → streak_days不变

验收：连续7天Streak=1.05 / 中断一天归零

**Step 1.6**：AI真实性评判

调用大模型评估善行内容 → 返回 high/medium/low/suspicious。
结果不向用户展示（静默因子）。

验收：详细内容→high / 泛化内容→low / 不合理的→suspicious

**Step 1.7**：善行发布接口增强

POST /deeds 响应增加 `feedback` 字段：
```json
{
  "feedback": {
    "fortune_change": 10,
    "total_fortune": 258,
    "weekly_deed_count": 3,
    "new_title": null,
    "milestone": null,
    "persona_task_id": "task_abc"
  }
}
```

验收：发布后feedback字段完整 / persona_task_id非空

**Step 1.8**：AI共鸣异步 + 轮询

- 发布时异步启动AI生成，返回task_id
- 前端轮询 `GET /deeds/:id/persona-response`
- 状态：pending → completed / timeout
- 超时fallback文案："AI小伙伴今天有点忙，但你的温暖已经被记住了 ✨"

验收：异步不阻塞 / 轮询可取到结果 / 超时fallback生效

**Step 1.9**：更新配置中心

- 删除：leaderboard相关配置项
- 新增：warmth参数（聚合阈值/刷新间隔/故事分页）+ fortune参数（各上限值）

验收：旧配置已清 / 新配置可读写

---

### Phase 2：前端温暖聚合（1.5天 | 依赖 Phase 1）

**Step 2.1**：温暖聚合API调用层
- 封装 `/warmth/stats`、`/warmth/personal`、`/warmth/map`、`/deeds?is_warmth_featured=true` 四个调用
- 含错误处理

**Step 2.2**：全国温暖卡片
- count-up数字动画，人数<10→"温暖正在发生…"

**Step 2.3**：区域温暖卡片 + 区域切换器

**Step 2.4**：个人统计面板
- 🔒仅自己可见标识 + 鉴权

**Step 2.5**：温暖故事列表
- 卡片含内容摘要/AI金句/匿名发布者/"💛被温暖到"计数

**Step 2.6**：温暖地图页（Web端）
- 中国地图色温图，不显示排名

**Step 2.7**：导航Tab「排行榜」→「温暖」+ 路由重定向

**Step 2.8**：个人统计页增强
- 自己看自己 → 显示温暖统计面板；看他人 → 不显示

验收：页面不出现排名字样 / 仅自己可见正确 / 所有卡片正常展示

---

### Phase 3：即时正反馈（1天 | 可与 Phase 2 并行）

**Step 3.1**：善行发布页动效序列

```
发布成功 → 遮罩渐入(0.1s) 
       → 福气飘字动画(0.3s) 
       → 周计数文字(0.2s) 
       → AI卡片"正在感受…"loading(0.5s) 
       → AI文字流式输出(1-2s) 
       → 分享/返回按钮
```

**Step 3.2**：AI文字流式输出 — 逐字追加 + 跳过按钮

**Step 3.3**：里程碑弹出 — 跨称号阈值触发，3秒自动消失

**Step 3.4**：首页「今日温暖」轻卡片 — 信息流顶部，点击跳转温暖页

验收：<3秒完成 / AI超时降级 / 里程碑触发 / 动效完整

---

### Phase 4：温暖故事 + 三端同步（0.5天）

**Step 4.1**：管理后台温暖故事标记 — 善行列表新增"⭐ 标记温暖故事"按钮

**Step 4.2**：温暖故事API — `GET /deeds?is_warmth_featured=true`

**Step 4.3**：小程序改造
- 温暖聚合页 / 底部Tab / 即时正反馈 / 设置开关
- 小程序特有：订阅消息模板申请

**Step 4.4**：App改造
- 同Web改造 + App推送权限配置

**Step 4.5**：管理后台
- 温暖故事管理页 / 配置中心 / 数据看板新增温暖指标

---

### Phase 5：测试与验收（1天 | 依赖全部）

**接口测试**：
- [ ] 温暖聚合各参数组合
- [ ] 个人统计登录/未登录
- [ ] 隐私保护（人数<10→null + `show_in_aggregation=false`排除）
- [ ] 福气计算各系数组合
- [ ] AI共鸣轮询各状态
- [ ] 温暖故事标记/查询

**端到端测试**：
- [ ] 发布公开善行 → 聚合+1
- [ ] `show_in_aggregation=false` → 聚合不变
- [ ] 私密善行 → 聚合不变
- [ ] 标记温暖故事 → 前端展示
- [ ] 即时反馈序列完整
- [ ] 第6条福气=0 / 超60被截断
- [ ] 里程碑触发

**兼容性**：
- [ ] 旧排行榜路径重定向
- [ ] 旧API仍可用
- [ ] 现有核心功能不受影响

**性能**：
- [ ] 聚合查询 < 200ms
- [ ] 发布提交 < 500ms
- [ ] AI共鸣 < 3秒（90分位）
- [ ] 温暖聚合页首屏 < 1s

**回归**：
- [ ] 善行发布/编辑/删除
- [ ] 信息流/点赞/评论
- [ ] 公益需求/接单/互评
- [ ] 挑战系统
- [ ] 用户注册/登录/设置
- [ ] 管理后台各功能

---

## 附录A：回滚方案

| 步骤 | 操作 |
|:---:|------|
| 1 | 前端路由 `/warmth` 指回旧排行榜组件 |
| 2 | 导航Tab「温暖」改回「排行榜」 |
| 3 | 关闭新聚合接口，恢复旧排行榜接口 |
| 4 | 如需：执行 Phase 0.5 回滚SQL |
| 5 | 如需：前端代码回滚到改造前 |

**触发回滚条件**：严重数据错误 / 旧客户端大面积不可用 / 性能严重下降。

## 附录B：关键约束速查

| 约束 | 实现要求 |
|------|------|
| 不排名 | API响应不出现 `rank`/`position`/`排名` |
| 人数<10 | `participant_count = null`，前端显示"10人以内" |
| 默认私密 | 善行发布 `is_public = false`（默认值） |
| 异步AI | AI生成不阻塞HTTP响应 |
| 防刷 | 日5条/日60/单30 三道上限 |
| 向后兼容 | 旧接口标记deprecated，保留至少2版本 |
| 仅自己可见 | 个人统计API强鉴权，不跨用户返回数据 |

---

*文档版本：v1.0 | 前置文档：SPEC v4 | 2026-06-19*
